# Google Gemini API key — used ONLY by the server-side function (api/chat.js
# on Vercel, netlify/functions/chat.js on Netlify). It is never exposed to the
# browser. Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
